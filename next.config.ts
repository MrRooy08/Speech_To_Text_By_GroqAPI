import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com"], // Thêm domain vào danh sách cho phép
  },
};

export default nextConfig;
